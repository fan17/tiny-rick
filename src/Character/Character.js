export default class Character {
    constructor(id, name, species, origin, image) {
        this.id = id
        this.name = name
        this.species = species
        this.origin = origin
        this.image = image
    }
}
